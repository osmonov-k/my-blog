name: Deploy Frontend to Vercel

on:
  push:
    branches: [main]
    paths:
      - "client/**"
      - ".github/workflows/deploy.yml"

permissions:
  contents: read
  deployments: write

concurrency:
  group: vercel-frontend-deploy
  cancel-in-progress: true

env:
  VERCEL_ORG_ID: ${{ secrets.VERCEL_ORG_ID }} # team_...
  VERCEL_PROJECT_ID: ${{ secrets.VERCEL_PROJECT_ID }} # prj_...

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node.js (for CLI only)
        uses: actions/setup-node@v4
        with:
          node-version: "20.x"

      - name: Install Vercel CLI
        run: npm i -g vercel@latest

      # optional, but keeps .vercel/env.* in sync if you need them
      - name: Pull Vercel env (production)
        run: vercel pull --yes --environment=production --cwd ./client --token ${{ secrets.VERCEL_TOKEN }}

      # 🚀 Remote build & deploy (no local `vercel build`, so no ENOENT)
      - name: Deploy (production, server-side build)
        run: vercel deploy --prod --yes --cwd ./client --token ${{ secrets.VERCEL_TOKEN }}
